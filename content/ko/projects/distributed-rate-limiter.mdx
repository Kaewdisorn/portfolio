---
title: "프로덕션 CI/CD 및 인프라 자동화"
summary: "GCP, Azure, 온프레미스 환경에서 운영되는 B2B 모빌리티 플랫폼의 배포 파이프라인을 설계하고 운영했습니다. Docker 컨테이너화와 Jenkins 기반 CI/CD 워크플로우로 서비스당 배포 시간을 약 70% 단축하고, 배포 일관성을 개선하며 헬스체크 기반의 장애 조기 감지 체계를 도입했습니다."
role: "시니어 백엔드 엔지니어"
period: "2022.01 – 2025.10"
type: company
stack:
  - Docker
  - Docker Swarm
  - Jenkins
  - GitHub Actions
  - Nginx
  - HAProxy
  - GCP
  - Azure
featured: true
order: 2
seo:
  title: "프로덕션 CI/CD 및 인프라 자동화 | 포트폴리오"
  description: "Docker 컨테이너화와 Jenkins CI/CD 파이프라인으로 배포 시간 약 70% 단축 및 GCP, Azure, 온프레미스 환경의 배포 일관성 개선 사례."
problem: "GCP, Azure, 온프레미스 서버에서 환경별로 다른 배포 스크립트가 운영되면서 라이브러리 버전과 구성 파일의 환경 드리프트가 반복되었습니다. 각 릴리스는 수작업 단계와 팀원 조율이 필요했으며, 배포 후 서비스가 실제로 정상인지 확인하는 자동화된 체계가 없었습니다."
architecture: "서비스를 Docker로 컨테이너화하고 Docker Swarm으로 멀티 노드 배포를 오케스트레이션합니다. Jenkins 파이프라인이 빌드, 테스트, 이미지 배포, 스테이징 배포를 자동화합니다. 배포 후 헬스체크가 서비스 준비 상태를 확인하고 통과한 뒤에만 트래픽이 라우팅됩니다. Nginx가 리버스 프록시와 TLS 종단을, HAProxy가 다중 레플리카 서비스의 로드 밸런싱을 처리합니다."
decisions:
  - title: "Docker Swarm vs. Kubernetes"
    body: "Kubernetes는 더 풍부한 오케스트레이션 기능을 제공하지만 전담 DevOps 없는 소규모 팀에서 컨트롤 플레인 운영 비용이 상당합니다. Docker Swarm은 서비스 레벨 배포, 롤링 업데이트, 헬스체크 기반 재시작을 훨씬 낮은 운영 복잡도로 제공했습니다. 트레이드오프는 스케줄러 기능과 에코시스템이었고, 현재 규모에서는 수용 가능했습니다. 필요 시 Kubernetes로의 마이그레이션 경로는 열어두었습니다."
  - title: "컨테이너화를 배포 계약으로"
    body: "이전 방식은 서버에 SSH로 접속해 소스를 풀하고 프로세스를 재시작하는 방식이었습니다. 이 방식에서 환경 드리프트는 필연적으로 발생했습니다. Docker 이미지를 배포 아티팩트로 사용하면 CI에서 테스트한 것과 정확히 같은 런타임이 모든 환경에 배포됩니다. 환경별 값(DB URL, API 키 등)은 런타임 환경 변수로 주입하여 이미지는 환경에 독립적으로 유지했습니다."
  - title: "헬스체크를 파이프라인 게이트로"
    body: "핵심 인식의 전환은 배포 후 상태를 관찰 대상이 아닌 차단 조건으로 취급하는 것이었습니다. 서비스가 비정상 상태로 배포되면 Jenkins 스테이지가 실패하고, 파이프라인이 멈추며, 비정상 인스턴스가 실제 트래픽을 받기 전에 팀이 알림을 받습니다. 이는 장애 감지 시점을 '누군가 프로덕션에서 이상을 발견'에서 '배포 시점에 파이프라인이 즉시 알림'으로 앞당겼습니다."
challenges:
  - title: "세 환경에 걸친 배포 일관성"
    body: "GCP, Azure, 온프레미스는 각기 다른 네트워크 구성, 방화벽 규칙, 사용 가능한 도구를 가지고 있었으며, 각 환경의 배포 스크립트가 독립적으로 발전해 있었습니다. Jenkins 공유 라이브러리를 사용해 환경별 배포 명령을 공통 인터페이스 뒤로 추상화하고, 환경 값은 Jenkins 크리덴셜로 저장해 배포 시점에 주입했습니다. 세 개의 환경별 스크립트가 하나의 파라미터화된 파이프라인으로 통합되었습니다."
  - title: "시작 시점 장애의 배포 단계 감지"
    body: "Node.js 서비스가 프로세스는 정상 기동했지만 실제로는 잘못 구성된 경우가 있었습니다. 특정 코드 경로에서만 접근하는 환경 변수 누락, 지연 초기화로 첫 사용 시에만 실패하는 DB 연결이 그 예입니다. 각 서비스에 GET /health 엔드포인트를 구현해 시작 시 DB 연결, 필수 환경 변수, 외부 의존성을 동기적으로 검증하도록 했습니다. Jenkins 배포 스테이지는 이 엔드포인트가 200을 반환할 때까지 대기하고, 그렇지 않으면 파이프라인을 실패시킵니다."
impact:
  - "CI/CD 자동화 후 서비스당 배포 시간 약 70% 단축"
  - "배포 일관성 개선 — 환경별 스크립트 분기 제거, Docker 기반 단일 파이프라인으로 통일"
  - "파이프라인 레벨 헬스체크 게이트로 배포 오류의 프로덕션 도달 감소"
  - "GCP, Azure, 온프레미스에서 통일된 배포 도구로 인프라 운영"
  - "헬스체크 및 알림 체계 도입으로 장애 감지 및 대응 시간 개선"
---

## 개요

Fatos에서 운영한 B2B 모빌리티 플랫폼의 인프라 자동화 및 배포 파이프라인 구축 사례입니다. 환경별 수작업 스크립트와 구성 드리프트에서 시작해, Docker 컨테이너화와 Jenkins CI/CD 파이프라인으로 세 개 클라우드 환경에 걸쳐 일관되고 빠르며 검증 가능한 배포 체계를 구축했습니다.

## 문제

플랫폼은 세 개의 독립된 인프라 환경에서 운영되었습니다: GCP, Azure, 온프레미스 서버. 각 환경은 역사적으로 자체 배포 스크립트, 도구 버전, 구성 패턴을 가지고 있었습니다. 서비스 업데이트를 배포하려면 대상 환경의 절차를 수작업으로 따라가고, 팀원 간 타이밍을 조율하며, 상태를 수동으로 확인해야 했습니다.

두 가지 구조적 문제가 시간이 지날수록 심화되었습니다.

**공유 배포 아티팩트 없음.** 각 서비스는 환경별 셸 스크립트로 소스 코드를 풀하고 프로세스를 재시작하는 방식으로 배포되었습니다. 환경 간 소스 드리프트가 프로덕션에서만 재현되는 버그의 반복적인 원인이었습니다.

**배포 후 자동 검증 없음.** 스크립트가 종료 코드 0으로 완료되면 배포가 완료된 것으로 간주했고, 실제 서비스 상태는 확인하지 않았습니다.

## 역할

인프라, 컨테이너화, CI/CD 파이프라인 설계를 전담했습니다. Dockerfile 및 Docker Compose 작성, 서비스별 Jenkinsfile 구축, Docker Swarm 오케스트레이션 구성, Nginx 및 HAProxy 설정, 헬스체크 및 알림 메커니즘 구현을 모두 직접 수행했습니다.

## 아키텍처

```mermaid
flowchart TD
    GH["GitHub\n푸시 / PR"] --> JK["Jenkins 파이프라인\n빌드 → 테스트 → 푸시 → 배포"]
    JK --> DR["Docker 레지스트리\n버전 이미지"]
    DR -->|이미지 풀| GCP["GCP 환경\nDocker Swarm"]
    DR -->|이미지 풀| AZ["Azure 환경\nDocker Swarm"]
    DR -->|이미지 풀| OP["온프레미스\nDocker Swarm"]
    GCP --> HC["헬스체크\n준비 상태 프로브"]
    AZ --> HC
    OP --> HC
    HC -->|통과| NG["Nginx / HAProxy\n신규 버전으로 트래픽 라우팅"]
    HC -->|실패| ALERT["알림: 파이프라인 실패\n롤백 트리거"]
```

각 서비스는 버전 이미지를 생성하는 Dockerfile을 갖습니다. Jenkins가 GitHub에서 코드를 가져와 빌드 및 테스트 후 레지스트리에 이미지를 푸시하고, 대상 환경에 배포를 오케스트레이션합니다. Docker Swarm이 환경 내에서 롤링 업데이트를 처리하고, 배포 후 헬스체크가 서비스 준비 상태를 확인합니다.

## 핵심 결정

### Docker Swarm vs. Kubernetes

Kubernetes는 오토스케일링, 세밀한 네트워크 정책 등 더 많은 기능을 제공하지만 컨트롤 플레인 운영 자체에 상당한 운영 부담이 따릅니다. Docker Swarm은 서비스 레벨 오케스트레이션, 롤링 업데이트, 헬스체크 기반 재시작을 전담 DevOps 없이도 백엔드 엔지니어가 운영할 수 있는 복잡도로 제공합니다. 트레이드오프는 스케줄러 기능과 에코시스템이었고, 현재 팀 규모와 요구사항에서는 수용 가능했습니다.

### 컨테이너화를 배포 계약으로

이전에는 각 서비스가 서버에서 직접 소스를 풀하고 재시작하는 방식으로 배포되었습니다. 시스템 패키지, 라이브러리 버전, 설정 파일이 환경마다 독립적으로 변화하면서 환경 드리프트가 필연적으로 발생했습니다. Docker 이미지를 배포 아티팩트로 사용하면 CI에서 검증한 것과 정확히 같은 런타임이 모든 환경에 배포됩니다. 환경별 값은 런타임 환경 변수로 주입하여 이미지 자체는 환경에 독립적으로 유지했습니다.

### 헬스체크를 파이프라인 게이트로

배포가 성공해도 서비스가 실제로는 비정상일 수 있습니다. 없는 환경 변수, 잘못된 DB 연결 문자열, 시작 시 경쟁 조건이 그 예입니다. Jenkins 배포 스테이지에 헬스 엔드포인트 프로브를 추가하여 서비스가 정상 상태를 반환할 때만 다음 스테이지로 진행하도록 변경했습니다. 이전에는 프로덕션에서 발견되던 장애가 파이프라인 스테이지에서 감지되기 시작했습니다.

## 도전과 해결

### 세 환경에 걸친 배포 일관성

GCP, Azure, 온프레미스는 서로 다른 네트워크 구성과 방화벽 규칙을 가지고 있었으며, 각 환경의 배포 스크립트가 독립적으로 발전해 있었습니다. Jenkins 공유 라이브러리를 사용해 환경별 배포 명령을 공통 인터페이스 뒤로 추상화했습니다. 환경 값은 Jenkins 크리덴셜로 저장하고 배포 시점에 주입했습니다. 이 변경으로 세 개의 환경별 스크립트가 하나의 파이프라인과 환경 파라미터로 통합되었습니다.

### 시작 시점 장애 감지

Node.js 서비스가 프로세스는 정상 기동했지만 실제로는 잘못 구성된 경우가 있었습니다. 데이터베이스 연결이 지연 초기화되어 첫 요청 시에만 실패하거나, 특정 코드 경로에서만 접근하는 환경 변수가 없는 경우가 그 예입니다. 프로세스가 실행 중이므로 단순한 프로세스 확인은 정상을 반환했습니다.

각 서비스에 `GET /health` 엔드포인트를 구현했습니다. 이 엔드포인트는 시작 시 DB 연결, 필수 환경 변수, 외부 의존성을 동기적으로 검증합니다. Jenkins 배포 스테이지는 이 엔드포인트가 200을 반환할 때까지 대기하고, 설정한 준비 상태 시간 내에 정상이 되지 않으면 파이프라인을 실패시킵니다. 이전에는 프로덕션에서 발견되던 장애가 파이프라인 스테이지에서 감지되기 시작했습니다.

## 성과

Fatos 운영 기간(2022–2025) 동안의 결과입니다:

- 서비스당 배포 시간: CI/CD 자동화 후 **약 70% 단축**
- 배포 일관성: 환경별 스크립트 분기 제거, Docker 기반 단일 파이프라인으로 통일
- 프로덕션 배포 오류: 파이프라인 레벨 헬스체크로 조기 감지 체계 구축
- **GCP, Azure, 온프레미스** 세 환경에서 통일된 배포 도구 운영
- 헬스체크 및 알림 체계 도입으로 장애 대응 시간 개선
